project 'QuickStartTemplate.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '13.0'
target 'QuickStartTemplate' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   # Pods for QuickStartTemplate
    
  pod 'Alamofire'
  pod 'Kingfisher'
  pod 'SwiftKeychainWrapper'
  pod 'URLNavigator'
  pod 'Hero'
end
